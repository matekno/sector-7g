# Base de datos PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# API Key para autenticacion de todos los endpoints /api/*
# Usar en Claude Desktop config: "BACKLOG_API_KEY": "valor-de-aqui"
API_KEY="sk-sector7g-CHANGE-THIS"

# URL publica de la app (para links en respuestas del MCP)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Directorio de uploads (relativo al directorio app/)
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE_MB="10"
